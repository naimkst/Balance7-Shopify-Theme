<!-- details-area -->
<div class="b-section details-area section-padding pt-0">
  <div class="container-bt">
    <div class="row">
      <div class="col-lg-6">
        <div class="section-title">
          <span>{{ section.settings.section_slug }}</span>
          <h2>{{ section.settings.section_title }}</h2>
          <p>{{ section.settings.section_description }}</p>
        </div>
      </div>
    </div>
    <div class="details-wrap details-active owl-carousel">

      {% for block in section.blocks %}
        <div class="details-item">
          <div class="row align-items-end">
            <div class="col-lg-6">
              <div class="details-content wow fadeInLeftSlow" data-wow-duration="{{ forloop.index |  plus: 1 }}000ms">
                <h2>{{ block.settings.feature_title }}</h2>
                {{ block.settings.feature_description }}
                {% if block.settings.link_text != blank %}
                  <a href="{{ block.settings.url }}">{{ block.settings.link_text }}</a>
                {% endif %}
              </div>
            </div>
            <div class="col-lg-6">
              <div class="details-img wow fadeInRightSlow" data-wow-duration="1{{ forloop.index |  plus: 2 }}00ms">
                <img
                  src="{{ block.settings.feature_images |  image_url: width: 900 }}"
                  width=""
                  height=""
                  alt="">
              </div>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
  {
    "name": "Landing Details Section",
    "settings": [
      {
        "type": "text",
        "id": "section_slug",
        "label": "Section Slug",
        "default": "Balance7™"
      }, {
        "type": "text",
        "id": "section_title",
        "label": "Section Title",
        "default": "More Details"
      }, {
        "type": "text",
        "id": "section_description",
        "label": "Section Description",
        "default": "Unlock your full health potential in just 3 days with Balance7's proven alkaline formula—trusted by over six million globally."
      }
    ],
    "blocks": [
      {
        "name": "Add Item",
        "type": "block",
        "settings": [
          {
            "type": "image_picker",
            "id": "feature_images",
            "label": "Image"
          },
          {
            "type": "text",
            "id": "feature_title",
            "label": "Title",
            "default": "More Energy"
          },
          {
            "type": "richtext",
            "id": "feature_description",
            "label": "Description",
            "default": "<p>Balance7™ harnesses the power of all-natural ingredients, aligned with scientific insights, to trigger a profound bodily response.</p> <p>This begins with the neutralization of stomach acid, which then prompts your body to elevate its alkaline levels, preparing your immune system to act, strong and alert.</p>"
          },
          {
            "type": "text",
            "id": "link_text",
            "label": "Link Text"
          }, {
            "type": "url",
            "id": "url",
            "label": "Link"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Landing Details Section",
        "blocks": [
          {
            "type": "block"
          }
        ]
      }
    ]
  }
{% endschema %}