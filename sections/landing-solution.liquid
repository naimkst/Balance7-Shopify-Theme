<!-- solution-area -->
<div class="b-section solution-area section-padding">
  <div class="container-bt">
    <div class="row">
      <div class="col-lg-6">
        <div class="section-title">
          <span class="wow fadeInLeftSlow" data-wow-duration="1000ms">{{ section.settings.section_slug }}</span>
          <h2 class="wow fadeInLeftSlow" data-wow-duration="1000ms">{{ section.settings.section_title }}</h2>
        </div>
      </div>
    </div>
    <div class="tab-content">
      {% for block in section.blocks %}
        <div
          role="tabpanel"
          class="tab-pane {% if forloop.first %} active {% endif %}"
          id="{{ block.settings.tab_name | handle}}">
          <div class="solution-wrap">
            <div class="solution-item">
              <div class="row align-items-center">
                <div class="col-lg-6">
                  <div class="solution-content">
                    <h2>{{ block.settings.title }}</h2>
                    {{ block.settings.description }}
                  </div>
                </div>
                <div class="col-lg-6">
                  <div class="solution-img">
                    <img
                      src="{{ block.settings.thumbnail |  image_url: width: 700 }}"
                      alt=""
                      width=""
                      height="">
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>

    <div class="solution-tab">
      <ul
        class="nav nav-tabs"
        id="myTab"
        role="tablist">
        {% for block in section.blocks %}
          <li class="nav-item" role="presentation">
            <a
              class="nav-link  {% if forloop.first %} active {% endif %}"
              id="{{ block.settings.tab_name | handle}}-tab"
              data-bs-toggle="tab"
              href="#{{ block.settings.tab_name | handle}}"
              role="tab"
              aria-controls="{{ block.settings.tab_name | handle}}"
              aria-selected="{% if forloop.first %}true{% else %}false{% endif %}">{{ block.settings.tab_name }}</a>
          </li>
        {% endfor %}
      </ul>
    </div>
  </div>
</div>


{% schema %}
  {
    "name": "Landing Solution Section",
    "settings": [
      {
        "type": "text",
        "id": "section_slug",
        "label": "Section Slug",
        "default": "Proven Results, Natural Solution"
      }, {
        "type": "text",
        "id": "section_title",
        "label": "Section Title",
        "default": "Why You Need pH Balance"
      }
    ],
    "blocks": [
      {
        "name": "Add Banner Images",
        "type": "block",
        "settings": [
          {
            "type": "text",
            "id": "tab_name",
            "label": "Tab Button Name",
            "default": "Immunity"
          }, {
            "type": "image_picker",
            "id": "thumbnail",
            "label": "Image"
          }, {
            "type": "text",
            "id": "title",
            "label": "Title",
            "default": "Immunity and pH Balance"
          }, {
            "type": "richtext",
            "id": "description",
            "label": "Description",
            "default": "<p>Our bodies seek a balanced pH. A tilt towards acidity can weaken our immunity, leaving us vulnerable. Balance7™ ensures this balance, fortifying immunity and ensuring robust health.</p><p>The choices we make, from food to our way of life and stress, can add acidity to our bodies. This silent disruptor weakens our immunity and can be the root cause of many health concerns.</p><p>Substances in Balance7™ are not just alkaline; they're immunity superheroes. They counter excessive acidity and support a healthier you.</p>"
          }
        ]
      }
    ],

    "presets": [
      {
        "name": "Landing Solution Section",
        "blocks": [
          {
            "type": "block"
          }
        ]
      }
    ]
  }
{% endschema %}